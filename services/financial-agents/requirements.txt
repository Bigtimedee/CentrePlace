fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.3

# TradingAgents (TauricResearch) — install from GitHub (not on PyPI)
git+https://github.com/TauricResearch/TradingAgents.git

# FinRobot (AI4Finance-Foundation) — optional enrichment layer
# Comment out if you don't want the longer install time
finrobot==0.1.5
pyautogen==0.3.1

# ai-hedge-fund (virattt) — LangGraph investor persona pipeline
# Not pip-installable; src/ directory is copied into the container at build time
# (see Dockerfile). These are its runtime dependencies:
langchain==0.3.7
langchain-core>=0.3.0
langchain-anthropic==0.3.5
langgraph==0.2.56
pandas>=2.1.0
numpy>=1.24.0
python-dotenv==1.0.0
colorama>=0.4.6
tabulate>=0.9.0
rich>=13.9.4
