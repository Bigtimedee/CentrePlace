fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.3

# TradingAgents (TauricResearch) — install from GitHub (not on PyPI)
# TradingAgents pinned to v0.2.3 — prevents HEAD drift and known dep conflicts
# Commit: 4641c03340c70e0e75e74234c998325164c72b36
git+https://github.com/TauricResearch/TradingAgents.git@v0.2.3


# ai-hedge-fund (virattt) — LangGraph investor persona pipeline
# Not pip-installable; src/ directory is copied into the container at build time
# (see Dockerfile). These are its runtime dependencies:
langchain>=0.3.12,<0.4.0
# Pin langchain-core to 0.3.x — without an upper bound pip can resolve 0.4.x
# which is incompatible with langchain 0.3.x and langchain-anthropic 0.3.x
# langchain-core floor raised to 0.3.81 — required by TradingAgents v0.2.3
langchain-core>=0.3.81,<0.4.0
langchain-anthropic>=0.3.15
# langgraph floor raised to 0.4.8 — required by TradingAgents v0.2.3 (was pinned 0.2.56)
langgraph>=0.4.8,<0.5.0
pandas>=2.3.0
numpy>=1.24.0
python-dotenv==1.0.0
colorama>=0.4.6
tabulate>=0.9.0
rich>=14.0.0

# FinRobot (AI4Finance-Foundation) + autogen intentionally omitted —
# pyautogen 0.2.x dep tree not verified against langchain-core>=0.3.81
# and langgraph>=0.4.8. The ImportError in main.py is caught silently.
# TODO: re-enable finrobot==0.1.5 + pyautogen~=0.2.35 after compat check.
